[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "createcards"
version = "0.1.0"

[project.scripts]
createcards = "createcards.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

